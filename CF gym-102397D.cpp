#include<iostream>
#include<algorithm>
using namespace std;
int main()
{
    long x,y,sum=0,c=0,s=0;
    cin>>x>>y;
    long arr[x];

    for(int i=0; i<x; i++)
    {
        cin>>arr[i];
        sum+=arr[i];
    }
  ///did not solve

}
